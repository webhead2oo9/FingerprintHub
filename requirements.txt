# FingerprintHub requirements (Python 3.12-3.14)
#
# Runtime and test dependencies are fully pinned for reproducible installs.
# The service never decodes images or computes perceptual hashes; clients do
# that work and send ``phash_hex`` text.

# Direct runtime dependencies
aiohttp==3.14.3
python-dotenv==1.2.3
psycopg==3.3.4
psycopg-binary==3.3.4
psycopg-pool==3.3.1
cryptography==50.0.0
alembic==1.19.1
SQLAlchemy==2.0.52
Mako==1.4.1
MarkupSafe==3.0.3
greenlet==3.5.5

# Direct test dependencies
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
coverage==7.15.4
Pygments==2.21.0

# Transitive dependencies required by the pinned direct set
aiohappyeyeballs==2.7.1
aiosignal==1.4.0
attrs==26.1.0
cffi==2.1.1
frozenlist==1.8.0
idna==3.19
iniconfig==2.3.0
multidict==6.7.1
packaging==26.3
pluggy==1.6.0
pycparser==3.0
propcache==0.5.2
typing_extensions==4.16.0
yarl==1.24.5

# Platform-conditional transitive dependencies
colorama==0.4.6; sys_platform == "win32"
tzdata==2026.3; sys_platform == "win32"
